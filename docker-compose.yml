services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: pg-biblioteca
    environment:
      POSTGRES_USER: federico
      POSTGRES_PASSWORD: proceso_seguro_2026
      POSTGRES_DB: biblioteca_seguridad
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init_pgvector.sql:/docker-entrypoint-initdb.d/10-pgvector.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U federico -d biblioteca_seguridad"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - biblioteca

  api:
    build: .
    container_name: api-biblioteca
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: "postgresql+psycopg://federico:proceso_seguro_2026@postgres:5432/biblioteca_seguridad"
      GROQ_API_KEY: "${GROQ_API_KEY}"
      GROQ_MODEL: "openai/gpt-oss-120b"
    env_file: .env
    ports:
      - "8000:8000"
    networks:
      - biblioteca
    volumes:
      - .:/app

volumes:
  postgres_data:

networks:
  biblioteca:
    driver: bridge
